# Asterisk Configs
This is a part of the extensions.conf file I use on my FreePBX instance. It will record to a file then upload it to the server to be sent to Discord.
You should probably add messages to it to tell the caller what's happening and how to use it.